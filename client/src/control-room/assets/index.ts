/**
 * The ASSETS page's public surface — the three region components the shell mounts.
 *
 * The shell imports this module and nothing else inside `assets/`, so everything else here can be
 * reorganised without touching `shell/pages.ts`.
 */
export { AssetsPage } from "./AssetsPage";
export { AssetsNavigator } from "./AssetsNavigator";
export { AssetsInspector } from "./AssetsInspector";
